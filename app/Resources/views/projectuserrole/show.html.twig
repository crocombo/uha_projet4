{% extends 'base.html.twig' %}

{% block body %}
    <h1>Projectuserrole</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projectUserRole.id }}</td>
            </tr>
            <tr>
                <th>Projectid</th>
                <td>{{ projectUserRole.projectId }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ projectUserRole.userId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projectuserrole_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('projectuserrole_edit', { 'id': projectUserRole.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
